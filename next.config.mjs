/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.unsplash.com',
      'media.istockphoto.com',
      'source.unsplash.com',
    ],
  },
};

export default nextConfig;
